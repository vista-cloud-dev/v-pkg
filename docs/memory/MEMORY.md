# v-pkg — per-repo memory index

One line per memory file. Content lives in the files, not here.

- kids-coverage-analysis → **canon is the proposal** `../proposals/v-pkg-kids-coverage-analysis.md` (adversarial KIDS coverage analysis; Track A install-fidelity vs Track B authoring). Live gotchas: [[install-fidelity-spike]], [[reversibility-classifier]], [[fileman-dd-component]], [[krn-param-def-component]].
- [clikit-grouped-help](clikit-grouped-help.md) — grouped help via `group:""` tags (KIDS-lifecycle categories) + the `explore` palette; mount `ExploreCmd` at the standalone root; v-cli must co-pin clikit + a v-pkg tag that carries the groups.
- [bespoke-installer-forbidden](bespoke-installer-forbidden.md) — **⛔ HARD DIRECTIVE (owner 2026-06-25):** NEVER a bespoke installer/patcher; install + back-out is strictly `v pkg install` / `uninstall` of a real KIDS build.
- [fu5b2-xwbbrk-wrapsplice](fu5b2-xwbbrk-wrapsplice.md) — **⛔ TOMBSTONE: the DELETED `wrap-rpc`/`internal/wrapsplice` XWBBRK patcher** (removed 2026-06-25, do NOT revive); lesson in [[bespoke-installer-forbidden]], mechanics in git history.
- [non-routine-reversal-resolution](non-routine-reversal-resolution.md) — non-routine components (params/DD/options) reverse via the AUTHORED back-out (`uninstall --backout`), not generic pre-image; `snapshot` itemizes `uncaptured` components rather than fake-capturing them.
- [pairing-verify-clean](pairing-verify-clean.md) — `install --auto-snapshot` writes the pre-image sidecar `<kid>.preimage.kids`; bare `uninstall` auto-restores it; `uninstall --verify` = verify-clean (exit 3 on dirty).
- [verify-drift](verify-drift.md) — `verify --drift` reads each shipped routine off the engine vs the patch's `RoutineSource` (line-2 canonicalized so checksum rewrites aren't false drift): applied/drifted/absent, exit 3.
- [component-type-coverage](component-type-coverage.md) — verify/uninstall presence + `^DIK` are registry-driven over `kids.Component` (1 row per type, not 11 hand-coded); `contentVerify` gate keeps unvalidated 0-node masks out of content-verify. Increment-1: +8 presence/uninstall-only types (INPUT TEMPLATE #.402 etc.); excludes #.5/#8993/#869.2/#9002226 (reasons in file); DIK safety live-proven net-zero on vehu. See [[verify-content]], [[class-aware-uninstall]].
- [verify-content](verify-content.md) — content-asserting `verify`: piece-aware live 0-node compare (`ZeroMatch`) + FILE DD field defs, not just `"B"`-index presence; per-type volatile-piece mask for FileMan transforms.
- [adversarial-stress-gate](adversarial-stress-gate.md) — `make stress`: full MSL+VSL lifecycle + adversarial refusal probes, **37/37 both engines**. KEY FIX: `verify --drift` false-positived on TAB-indented routines (engine flattens TAB→SPACE) — fixed via m-cli lint M-MOD-039 + v-stdlib detab. See [[verify-drift]], [[live-package-gate]].
- [live-package-gate](live-package-gate.md) — `make live-gate [NEG=1]`: MSL+VSL install→content-verify→backout→verify-clean + negative dependency, **11/11 both engines**. v-pkg load over docker needs `M_YDB_ROUTINES`.
- [package-footprint](package-footprint.md) — `install --register-package` writes the #9.4 footprint ($$PKGVER/$$PKGPAT^XPDIP); `uninstall --deregister` is the symmetric inverse so `$$PATCH^XPDUTL` stays honest. Gotcha: honesty needs a **≤4-char prefix**.
- [class-aware-install](class-aware-install.md) — no silent clobber: split shipped routines existing(overwrite)/greenfield, then `decideInstall` (greenfield→proceed; overwrite+`--snapshot`→capture pre-image; `--allow-overwrite`→unsafe; else REFUSE exit 4). Pairs with [[class-aware-uninstall]]/[[snapshot-restore]].
- [class-aware-uninstall](class-aware-uninstall.md) — `Classify` the .KID + route via `decideUninstall`: `--restore` (class-1) / `--backout` (class-2) / side-effecting+no-flags → REFUSE exit 4 (`--force` overrides) / class-1 → greenfield delete. **+`actPartition` (2026-06-30, v-rpc-tap BB1):** mixed overwrite-foreign+greenfield build → restore foreign FIRST, delete greenfield AFTER (keyed on the pre-image's routine set); idempotent (clears the snapshot `…PREIMAGE` #9.7); dual-engine proven (`scripts/partition-uninstall-gate.sh` 17/17 vehu+foia). Refuse decided offline. **+declared-foreign refuse (F1, 2026-06-30):** buildspec `foreignRoutines` → embedded `("VPKG","FOREIGN",…)` .KID node (stripped from engine staging by `kids.EnginePairs`) → bare uninstall with no pre-image REFUSES (never bricks the foreign routine); `--force` deletes greenfield subset only. **+national-overwrite verify (D4, 2026-06-30):** `uninstall --verify` grades declared-foreign restore via `kids.RoutineRestoreVerdict` → `foreignRestore: exact | command-clean-provenance-drift | drift` (command lines = checksum surface since both VistA checksums exclude line 2; line 2 = patch-history provenance). `scripts/foreign-refuse-gate.sh` 24/24 vehu+foia (asserts `exact`). **GOTCHA: foreignness is by DECLARATION, never a name-prefix heuristic** (package name ≠ routine namespace — MSL ships STD*).
- [snapshot-restore](snapshot-restore.md) — `snapshot` reads each patch routine off the live engine → pre-image `.KID` with an honest `completeUndo` flag (class-1 only); `restore` = install-of-pre-image, preview unless `--commit`.
- [reversibility-classifier](reversibility-classifier.md) — `Classify`/`ClassifyBuild` + `v pkg classify` derive PureOverwrite vs SideEffecting from the `.KID` alone (4 node-shape probes); `make corpus` round-trips all 2,404 WorldVistA KIDS (DRIFT=0). **KRN probe fixed 2026-06-30** (per-build `BLD..KRN..NM` excl #9.8, alongside top-level).
- verifiable-safety-hardening → **tracker is canon** `../verifiable-safety-tracker.md` (4-part effort from the kids-classifee/convention reports: (1) component coverage 11→all FileMan types incl INPUT TEMPLATE #.402 — verify+uninstall orphan them today; (2) pre-install dry-run/compare; (3) robustness — half-install heal/transport-checksum/sidecar integrity; (4) install attestation). Carries the live-verified per-type global-root table (`^DIC(f,0,"GL")`). Each = a TDD+dual-engine increment.
- kids-corpus-reversibility → **canon is the findings doc** `../design/kids-corpus-findings.md` (corpus evidence from 2,404 builds; node forms + class-detection probes; class shares 28%/72% post-correction).
- [routine-name-length-policy](routine-name-length-policy.md) — `Spec.AllowLongNames` (`allowLongNames` in the build spec): cap 8 (legacy) vs 31 (M engine limit); the M-naming regex + 31 ceiling still bind. Implements the org modern-routine-name ADR.
- [streamed-install](streamed-install.md) — `install` streams the transport global in bounded chunks → staging `^XTMP("VPKGI")` → MERGE + `EN^XPDIJ` in one process, fixing a silent partial-install of large packages; a count-guard refuses `stage-incomplete`.
- [install-fidelity-spike](install-fidelity-spike.md) — **canon `../archive/v-pkg-install-fidelity-spike.md`**: ground-truthed the KIDS phase boundary (`EN^XPDIJ` is filing-only); shipped **route (c) augmented direct-populate** (env-check + `$$NEWCP^XPDUTL` INI/INIT checkpoints + seeded #9.7 QUES); pre/post fire only if checkpoints exist. Live both engines. See [[kids-coverage-analysis]].
- [install-question-answers](install-question-answers.md) — `install --answer NAME=VALUE` pre-answers a build's install questions (read via the real `$$ANSWER^XPDIQ`); `"QUES"` is install scratch, not a #9.7 FileMan field. See [[install-hooks-authoring]], [[install-fidelity-spike]].
- [multi-build-install](multi-build-install.md) — `install` runs a multi-build (meta) distribution in `**KIDS**`-header order, stop-on-failure, each constituent independent (own #9.7/transport); build-specific flags refused for multi. See [[install-fidelity-spike]], [[package-footprint]].
- [install-hooks-authoring](install-hooks-authoring.md) — `v pkg build` AUTHORS env-check/pre/post install-hook routines (`"PRE"`/`"INI"`/`"INIT"`, mirrored under `"BLD",1`). Installer fix: **KILL `^XTMP("XPDI",XPDA)` before the MERGE** (stale REQB nodes falsely rejected). See [[install-fidelity-spike]].
- [multi-field-dd-emitter](multi-field-dd-emitter.md) — v-pkg authors a multi-field FileMan DD + file DATA (4 action codes a/m/o/r), live-install-proven both engines. Bug fixed: DD piece-3 root must drop the leading `^`. Permanent file numbers allowed (out-of-range needs explicit `globalRoot`). See [[fileman-dd-component]], [[kids-coverage-analysis]].
- [fileman-dd-component](fileman-dd-component.md) — builds/installs/verifies/uninstalls a brand-new FileMan FILE (its DD) as a KIDS FIA component (doubled `^DD` file-number level, options piece-3=`"f"`). Uninstall gotcha: take the whole `"GL"` value (starts with `^`), never `$P` on `^`.
- [option-entry-component](option-entry-component.md) — generic KRN entry-component emitter (`internal/kids/entrycomp.go`: OPTION #19 / SECURITY KEY #19.1 / PROTOCOL #101 / …); adding a type ≈30 lines. GOTCHA: `entryNames` must compare the file-number subscript NUMERICALLY (int OR float) — an `IsFloat`-only probe silently drops integer-numbered types on the live path (`subNum`). Extends [[krn-param-def-component]].
- [krn-param-def-component](krn-param-def-component.md) — builds/installs/verifies/uninstalls a #8989.51 PARAMETER DEFINITION as a KIDS KRN component (+ Required Builds #9.611). KEY FIX: seed `^XPD(9.7,XPDA,"KRN")` from the manifest or `KRN^XPDIK` GVUNDEFs (status stuck at 2).
- mixed-build-split-reversal → central docs `docs/proposals/v-pkg-mixed-build-split-reversal.md` (**D3 BUILT 2026-06-30 as [[class-aware-uninstall]]'s `actPartition`** — the ordered restore-foreign+delete-greenfield op, dual-engine proven; D1/D2 `foreignRoutines` declaration + the **mixed+no-sidecar→REFUSE** routing still PENDING (= the F1 P1b item — until then a bare `uninstall` with no sidecar still bricks a foreign routine); D4 national-overwrite verify pending). Stays inside the v-pkg lifecycle ([[bespoke-installer-forbidden]]); extends [[class-aware-uninstall]]/[[reversibility-classifier]]/[[snapshot-restore]].
