# v-pkg — per-repo memory index

One line per memory file. Content lives in the files, not here.

- kids-coverage-analysis → **canon is the proposal** `../proposals/v-pkg-kids-coverage-analysis.md` (adversarial coverage analysis, 2026-06-28: v-pkg authors ~4 of ~24 KIDS component types; "install 2000 packages" is install-fidelity Track A, not authoring Track B; F1 silent-drop red-gate; corpus re-tally). The memory dump was a duplicate — deleted, read the proposal. Live Track-A gotchas live in [[install-fidelity-spike]]; see [[reversibility-classifier]], [[fileman-dd-component]], [[krn-param-def-component]].
- [clikit-grouped-help](clikit-grouped-help.md) — 2026-06-26 (tagged v0.4.0): repinned clikit v0.1.0→v0.2.0 (styled grouped help renderer) + added `group:""` tags to `pkgcli.Commands` → KIDS-lifecycle categories (Inspect/Transform/Build & install/Back-out). v-cli must pin clikit v0.2.0 + v-pkg v0.4.0 to surface these under `v pkg`. Gates green.
- [bespoke-installer-forbidden](bespoke-installer-forbidden.md) — **⛔ HARD DIRECTIVE (owner 2026-06-25):** NEVER build/use a bespoke installer/patcher. Install + back-out = **strictly + exclusively** `v pkg install` / `v pkg uninstall` of a proper KIDS build (the tap ships in v-stdlib `kids/vsl.build.json`). The `wrap-rpc`/`internal/wrapsplice` `CALLP^XWBBRK` splice was DELETED. Shared: [[never-use-bespoke-installer]].
- [fu5b2-xwbbrk-wrapsplice](fu5b2-xwbbrk-wrapsplice.md) — **⛔ DELETED 2026-06-25 — historical record only; describes a REMOVED bespoke patcher, do NOT use/revive.** Was RPC→S3 tap FU-5 5B.2a (branch `s3tap-fu5b2-patcher`): the deleted **`internal/wrapsplice`** — pure-Go, off-engine patcher that inserts/backs-out the two `VSLRPCWRAP` traffic-tap side-calls in national **`CALLP^XWBBRK`** (delivery Option A: read stock off the engine → `Splice` host-side → ship patched routine via KIDS). **Content-anchored** (req = exact `S:$L($G(XWBSEC)) ERR="-1^"_XWBSEC`; resp = `D CAPI^XWBBRK2(` substring), **each required unique or `Validate` REFUSES = the FU-21 re-pin gate in code**. Idempotent + exactly reversible (every inserted line carries `Marker` `;VSLTAPW`; `Splice→Unsplice` restores stock byte-for-byte). `linePrefix` preserves statement level (req top-level after the denial line; resp at the dispatch dot level, inside the `:155` success block). Splice line is a plain `D req^VSLRPCWRAP` (no guard — VSLRPCWRAP owns the fence/gate). Table-driven vs the verbatim `CALLP^XWBBRK` 140-162 fixture; `make all` green, wrapsplice cov 97.7%. **5B.2b parts 1-2 DONE 2026-06-23**: `readRoutineSource` (first engine-READ capability in v-pkg — `$TEXT`-loop markers → ordered lines; injection-guarded) + **`v pkg wrap-rpc status|install|backout`** (read→splice→`MakeBuildPairs`; **PREVIEW by default**, live KIDS install only under `--commit`). Live read-only smoke on foia confirmed the patcher on the REAL 211-line XWBBRK (status `anchorsOk:true`; preview produced the 213-line patched routine, side-calls at 154/160). **STILL HELD (5B.2c, owner go-ahead): the live `--commit` install + the non-interference proof.** Shared: docs [[rpc-traffic-s3-streaming-proposal]]; splice doc `docs/discoveries/fu-5b-callp-splice.md`.
- [non-routine-reversal-resolution](non-routine-reversal-resolution.md) — **open Q2 resolved (2026-06-25)**: non-routine components (params/DD/options) reverse via the AUTHORED back-out (`uninstall --backout`), NOT generic pre-image (which would over-claim reversibility). `snapshot` itemizes `uncaptured` components instead of fake-capturing them (`uncapturedComponents`, unit-tested). Closes the reversibility lifecycle; only #9.7 provenance (Q1) remains.
- [pairing-verify-clean](pairing-verify-clean.md) — **pre-image pairing + verify-clean (2026-06-25)**: `install --auto-snapshot` writes the pre-image to the sidecar `<kid>.preimage.kids`; `uninstall` (no flags) auto-detects+restores it (`resolveAutoRestore`); `uninstall --verify` confirms the live routines match the re-applied artifact (verify-clean, exit 3 on dirty). Round-trip = `install --auto-snapshot` then `uninstall`. Unit-tested in pkgcli/pairing_test.go.
- [verify-drift](verify-drift.md) — **`v pkg verify --drift` (2026-06-25)**: FU-21 re-pin gate — reads each shipped routine off the engine, compares to the patch's `RoutineSource` via `RoutineDriftMatch` (line-2 canonicalized so checksum rewrites aren't false drift): applied/drifted/absent, exit 3 on drift. Live-proven on vehu both ways (patched .KID→drifted, pre-image→applied). Unit-tested in internal/kids/drift_test.go.
- [verify-content](verify-content.md) — **content-asserting `v pkg verify` (2026-06-29)**: verify now reads each shipped entry record's LIVE 0-node and compares piece-aware (`kids.ZeroMatch`), not just `"B"`-index presence — gating that the record we shipped is the record filed. Per-type volatile-piece mask on `entryType` for FileMan transforms (#771 country p7, #870 LLP p3). Separate `VerifyContentScript` (presence path untouched). **Live-proven 2026-06-29** (VSL `8989.51:VPNG GREETING`→ok on vehu via the [[live-package-gate]]).
- [live-package-gate](live-package-gate.md) — **live MSL+VSL "ship the real package" gate (2026-06-29)**: `scripts/live-package-gate.sh` + `make live-gate [NEG=1]`; builds MSL+VSL from real specs and drives install→content-verify→backout→verify-clean (+negative dependency test) through the driver. **YDB/vehu 11/11 PASS.** Env recipe gotcha: v-pkg load over docker needs `M_YDB_ROUTINES`. The #9.4-deregister gap it surfaced is now CLOSED by `uninstall --deregister`. IRIS not run this session.
- [package-footprint](package-footprint.md) — A.3 `install --register-package` writes the #9.4 footprint ($$PKGVER/$$PKGPAT^XPDIP); **`uninstall --deregister` (2026-06-29)** is the symmetric inverse — DIKs the #9.4901 PATCH APPLICATION HISTORY entry so `$$PATCH^XPDUTL` is honest after back-out (live-proven 1→0 on vehu; enables the negative Required-Build gate). ≤4-char prefix gotcha for $$PATCH honesty.
- [class-aware-install](class-aware-install.md) — **class-aware `v pkg install` (2026-06-25)**: no silent clobber — probes the engine (`captureRoutinePreimages`) to split shipped routines into existing(overwrite)/greenfield(new), then `decideInstall`: pure greenfield → proceed (old path); overwrite + `--snapshot` → auto-capture pre-image then install (enables uninstall --restore); `--allow-overwrite` → proceed unsafe; else → REFUSE exit 4. Live-proven on vehu (XWBBRK patch refused). 6-case decision test; `make contract` for new flags. Pairs with [[class-aware-uninstall]]/[[snapshot-restore]].
- [class-aware-uninstall](class-aware-uninstall.md) — **class-aware `v pkg uninstall` (2026-06-25)**: the safety fix — `Classify`s the `.KID`, routes via `decideUninstall`: `--restore` (class-1 pre-image) / `--backout` (class-2 authored inverse) / side-effecting+no-flags → REFUSE exit 4 (`--force` overrides) / class-1+no-flags → greenfield delete (old path). refuse decided offline (before engine). Smoke-proven on OOPS*2.0*22 (refused). 7-case decision unit test; `make contract` for new flags. Pairs with [[snapshot-restore]].
- [snapshot-restore](snapshot-restore.md) — **`v pkg snapshot`/`restore` (2026-06-25)**: pre-image reversal (class-1 leg). snapshot reads each routine the patch ships off the LIVE engine (`readRoutinePreimage` → present/absent split; absent=greenfield, no error) → pre-image `.KID` named `<orig> PREIMAGE`; HONEST `completeUndo` flag (true only for class-1, no greenfield/non-routine), else WARNS provenance-only. restore = install-of-pre-image (reuses `runInstall`), PREVIEW default, `--commit` overwrites. **Live-proven on vehu** (real 213-line XWBBRK: snapshot read+write+classify, restore preview). Tested vs fakeDriver; `make contract` for the 2 new verbs. Next: class-aware install/uninstall + verify --drift.
- [reversibility-classifier](reversibility-classifier.md) — **reversibility classifier + corpus harness (2026-06-25)**: `internal/kids/reversibility.go` (`Classify`/`ClassifyBuild`) + `v pkg classify` derive PureOverwrite vs SideEffecting from the `.KID` alone (4 node-shape probes: RTN name / BLD INI·INIT·PRE·PRET install-code / top-level KRN entry / BLD,4 FILE-multiple). `make corpus` (VPKG_KIDS_CORPUS, SKIPs in CI) round-trips ALL 2,404 WorldVistA KIDS (PASS=2404 DRIFT=0) + classifies each: pure=873(36%)/side=1531(63%), matching analyze.py. Keystone for the engine-bound snapshot/restore/class-aware-uninstall verbs. Adding the verb needed `make contract`.
- kids-corpus-reversibility → **canon is the findings doc** `../design/kids-corpus-findings.md` (corpus evidence from 2,404 WorldVistA `.KID` builds; node forms + class-detection probes; analyzer+clone in `~/data/kids-patches/`, uncommitted). The memory dump duplicated it and carried the pre-correction 35%/64% headline (now 28%/72% per the coverage-analysis F5) — deleted, read the findings doc.
- [routine-name-length-policy](routine-name-length-policy.md) — `v-pkg build` gained **`Spec.AllowLongNames`** (`allowLongNames` in `kids/<pkg>.build.json`): absent→legacy cap `RoutineNameMaxStd=8`, true→`RoutineNameMaxLong=31` (M engine limit). `isRoutineName(s,maxLen)` parameterized; M-naming regex + 31 ceiling still bind; `.kids` emit side never had a limit. Implements org ADR (modern routine-name length policy). buildspec cov 98.7%. Gotcha: `make test` needs `CGO_ENABLED=1` (Makefile hard-sets 0 but uses `-race`). Branch `routine-name-policy`. Shared: [[routine-name-length-policy]] (docs repo).
- [streamed-install](streamed-install.md) — `v pkg install` now streams the transport global in size-bounded chunks → staging global `^XTMP("VPKGI")` → MERGE + `EN^XPDIJ` in one process, fixing a silent partial-install of large packages (the one-mega-routine staging truncated at ~3 routines). YDB live-proven on the full 15-routine MSL base (test-in-place 15/15 suites). IRIS validation of the chunked path owed.
- [install-fidelity-spike](install-fidelity-spike.md) — **2026-06-28: Track A.1 spike** (`docs/proposals/v-pkg-install-fidelity-spike.md`) — ground-truthed the KIDS phase boundary from real `XPD*` source: **`EN^XPDIJ` is filing-only**; env-check (`ENV^XPDIL1`) + required-builds (`REQB`, only via `ENV`) + question-prompting (`EN^XPDI`) run earlier; pre/post routines fire in `EN^XPDIJ` **only if** the load-phase `INI`/`INIT` checkpoints exist (direct-populate never makes them → **pre/post silently skipped**). Unseeded standard question = `""` = safe-NO. **Recommends route (c) augmented direct-populate** (add real-phase calls `$$ENV^XPDIL1(1)`, `$$NEWCP^XPDUTL` checkpoints, seeded #9.7 QUES; A.1.1 pre/post first). Route (a) headless `EN^XPDI` rejected (un-suppressible prompts vs stdin-less `Exec`); (b) expect = fallback. **"Missing Developer Tools UG" blocker resolved** — it's a section of `krn_8_0_dg_kids_ug` (in corpus); no documented headless silent-install API exists. **A.1.1 DONE + LIVE-PROVEN 2026-06-28 on vehu (YDB) + foia-t12 (IRIS)**: `FinalInstallScript` emits real `$$NEWCP^XPDUTL` INI/INIT checkpoints before `EN^XPDIJ`; fixture `testdata/zza1-prepost/` shows `^ZZA1OUT("PRE"/"POST")=1` (both fired) under A.1.1 vs empty under the old binary (counterfactual). Driver gotcha: YDB `exec load` needs `M_YDB_ROUTINES=/home/vehu/r`; IRIS needs `M_IRIS_NAMESPACE=VISTA`. See [[kids-coverage-analysis]].
- [install-question-answers](install-question-answers.md) — **2026-06-28: A.1.3 — `v pkg install --answer NAME=VALUE` pre-answers a build's install questions** so a pre/post routine reads them via the real `$$ANSWER^XPDIQ` (the direct-populate path skips `EN^XPDIQ`). Seed = three internal nodes per answer (`,IEN,0)`=name, `,IEN,1)`=value, `"B",name,IEN)=""`); `"QUES"` is install **scratch, not a #9.7 FileMan field** (no header node). `FinalInstallScript` seeds after env-check, before `EN^XPDIJ`; `--answer` splits on first `=`, order-preserved. **Live-proven both engines** (`testdata/zza4-ques/`): `--answer ZZA4Q=HELLO` → `^ZZA4OUT("Q")="HELLO"`, control → `""`. `make contract` for the new flag. See [[install-hooks-authoring]], [[install-fidelity-spike]].
- [multi-build-install](multi-build-install.md) — **2026-06-28: A.4 — `v pkg install` installs a multi-build (meta) distribution**: each constituent in **`**KIDS**`-header order (which IS the install order, `^`-separated, preserved in `k.InstallNames`), stopping on first failure. Each installs independently (own #9.7/transport) via the same class-aware `liveInstall` (no parallel installer). `installSequence` loop; `runMulti` when `len(InstallNames)>1`. Build-specific flags refused for multi (`--register-package`, explicit `--snapshot`); `--auto-snapshot` per-build sidecar. **LIVE-PROVEN both engines** (ZZM1+ZZM2 merged via `WriteKID` multi-name header → both status 3 in order). Corpus `MultiBuilds/` dir; `MBREQ` = per-build metadata (doesn't block standalone install). VERIFY/UNINSTALL still single-build. See [[install-fidelity-spike]], [[package-footprint]].
- [package-footprint](package-footprint.md) — **2026-06-28: A.3 — `v pkg install --register-package "<NAME>"` writes the PACKAGE #9.4 footprint** (VERSION #9.49 + PATCH APPLICATION HISTORY #9.4901) via the REAL `$$PKGVER`/`$$PKGPAT^XPDIP` after `EN^XPDIJ`, find-or-creating the #9.4 entry by PREFIX, so downstream `$$PATCH^XPDUTL` is honest. **LIVE-PROVEN both engines** (vehu+foia-t12): `$$PATCH(ZZV*1.0*3)` **0→1**, negative `*99` stays 0. **KEY GOTCHA:** honesty needs a **≤4-char prefix** — `$$LKPKG^XPDUTL` resolves PREFIX only for `$L<5` (else by NAME), and `$$PATCH`'s own regex caps the prefix at 1-4 UN chars; a 6-char "ZZSKEL" filed fine but `$$PATCH` returned 0. Not hard-gated (documented). `make contract` for the new flag. See [[install-fidelity-spike]], [[install-hooks-authoring]].
- [install-hooks-authoring](install-hooks-authoring.md) — **2026-06-28: B.3 — `v pkg build` AUTHORS install-time hook routines** (envCheck→`"PRE")`, preInstall→`"INI")`, postInstall→`"INIT")`, mirrored under `"BLD",1`; shape-only validation; unset = byte-identical). **Live-proven end-to-end via `v pkg build` (no hand-injection) on BOTH engines** (vehu YDB + foia-t12 IRIS): positive ZZA3 → ENV/PRE/POST sentinels + #9.7 status 3; negative ZZA3R → `env-check-rejected^1^`, nothing filed (proves A.1.2's env-check-**routine** reject arm). Gotchas: env-check routine source MUST be ASCII (`^%ZOSF("SAVE")` compiles it); a rejected install rolls back its TP so the negative sentinel doesn't persist (read the marker, not a global). **Installer fix: KILL `^XTMP("XPDI",XPDA)` before the MERGE** in `FinalInstallScript` — a re-assigned #9.7 IEN left stale REQB nodes that made `$$REQB` falsely reject (regression test `TestFinalInstallScript_KillBeforeMerge`). Fixture `testdata/zza3-hooks/`. See [[install-fidelity-spike]], [[kids-coverage-analysis]].
- [multi-field-dd-emitter](multi-field-dd-emitter.md) — **2026-06-28: v-pkg AUTHORS a multi-field FileMan DD — now LIVE-INSTALL-PROVEN both engines** (install→register→file-a-record on vehu+foia-t12; `EVT-A^42^W^…` stored at the right pieces, `.01` xref, dierr=0). **Bug found+fixed by the live-prove:** the pointer field def shipped `RP200'^^VA(200,` (empty piece 3) because `PointRoot` carries a leading `^` (piece delimiter) — DD piece-3 wants the root with NO caret (`VA(200,`); `fieldDef` now strips it. Symptom was `FIA^XPDIK` NULSUBSC → install stuck at **status 2** with the DD present but the file half-registered (no `^DIC("B")`). Minor open gap: pointed-to back-ref `^DD(200,"PT",…)` not shipped (records still file fine). Earlier note (was "v-pkg now AUTHORS a multi-field DD") (the `.01` NAME + N typed fields: free text/numeric/date/set-of-codes/pointer, each `required`+help) — coverage-analysis item **B.2-a**, the v-stdlib R3 enabler. Grounded grammar from real export #8992.7 (`^DD(F,F,0)`=`FIELD^^hi^count` literal `FIELD`; def node = `LABEL^TYPE^set/ptr^node;piece^xform`; **no per-field GL node** — storage inline; `,N,"DT")`/`,21` WP omitted). **B.2-b DONE + LIVE-PROVEN both engines 2026-06-28:** file **DATA** ships under `("DATA",file,ien,node)` (raw record subtree); send-opts is **9 pieces**, **p7=`y`/`n` data switch + p8=action letter `a`/`m`/`o`/`r`** (ADD-IF-NEW/MERGE/OVERWRITE/REPLACE — straight off `^DD(9.64,222.8,0)`); install = `DATAIN^DIFROMS`→`EN^DIFROMS4` re-files via `I^DITR` + REINDEX (emitter ships data nodes only, xrefs rebuilt live). #999002/3-records/MERGE proven byte-identical vehu+foia-t12 (right pieces, "B" xref rebuilt, clean uninstall). **Permanent file numbers**: hard 999000–999999 gate → any positive integer, but out-of-range REQUIRES explicit `globalRoot` (governance stays an org namespace-registry concern). Gates green, corpus DRIFT=0, goldens `testdata/zzvslaudit`+`testdata/zzdata`. See [[fileman-dd-component]], [[kids-coverage-analysis]].
- [fileman-dd-component](fileman-dd-component.md) — `v pkg` now builds + installs/verifies/uninstalls a **brand-new FileMan FILE (its DD) as a KIDS FIA component** (VSL **M3.T1**, #999000 ZZVSLFS). FIA transport + the **DOUBLED `^DD` file-number level** + options **piece 3="f" (full, not "p")** for a new file + the install seed (`XPCK^XPDIK("FIA")` + `…,0,2)=1`). **Proven install→verify→uninstall→clean + record-files-with-xref on BOTH engines** (vehu YDB + foia-t12 IRIS). Uninstall gotcha: the `"GL"` value starts with `^` so take it whole, never `$P` on `^`.
- [option-entry-component](option-entry-component.md) — **2026-06-28: B.1 — generic KIDS entry-component emitter; OPTION (#19) + SECURITY KEY (#19.1) + PROTOCOL (#101) + unified multi-type manifest**. Adding a SEND/DELETE type is now ~30 lines (entryType + record packer + buildEntryGroups line + Names reader + verify/uninstall threading); 3 types landed on it. PROTOCOL mirrors OPTION's node skeleton (0-node NAME^ITEM TEXT^^TYPE, ENTRY node 20, EXIT node 15) but with its OWN data global `^ORD(101,`, OWN TYPE codes, and NO "U" xref. Each new type: ground-truth its ORD action-routine tail + data global + TYPE codes from the corpus/live DD, author a base record, live-prove both engines (`testdata/zzkey`, `testdata/zzproto`). Generalized the #8989.51 KRN path into `internal/kids/entrycomp.go` (`entryType`/`entryRec`/`imageNode`/`entryGroup` → shared manifest+ORD+record-image, `entryNames`); landed OPTION (author-from-spec, run-routine) AND **migrated param-def onto the same core + unified the `"BLD",1,"KRN",0)` header across types** so one build ships several KRN types (mixed OPTION+param-def proven). **Live install→verify→uninstall→clean BOTH engines** (`testdata/zzoption` single-type, `testdata/zzmix` mixed). **GOTCHA 1: `entryNames` must compare the file-number subscript NUMERICALLY (int OR float)** — integer file numbers (#19/#101/#8994) re-parse from a `.KID` as int, so an `IsFloat`-only probe (param-def hid it: 8989.51 has a dot) passed the in-memory test but silently dropped every option on the live path; `subNum()` fixes it (regression `TestBuild_OptionNames_AfterReparse`). **GOTCHA 2: KRN header "last IEN" is insertion-order in real KIDS (non-reproducible); v-pkg uses deterministic max(file#)+type-count — cosmetic to install (XPDIK iterates subscripts).** OPTION ORD action-routine line + `-1`=`0^1` are type-specific national constants; NM SEND `^^0` shared. Param-def migration byte-identical (zzparam golden + corpus DRIFT=0). Side-effecting → bare uninstall refuses, `--force` runs the `^DIC(19,`/`^XTV(8989.51,` DIK back-out. Extends [[krn-param-def-component]]; see [[kids-coverage-analysis]].
- [krn-param-def-component](krn-param-def-component.md) — `v pkg` now builds + installs/verifies/uninstalls a **#8989.51 PARAMETER DEFINITION as a KIDS KRN component** (+ Required Builds #9.611), the VSL/MSL **T1.3 enabler**. KRN transport = BLD manifest + `"ORD"` (xref=1) + top-level `"KRN",8989.51,seq` record image MERGEd into `^XTV` by `KRN^XPDIK`. **KEY FIX**: the direct-populate install must seed `^XPD(9.7,XPDA,"KRN")` from the build manifest or `KRN^XPDIK` GVUNDEFs → status stuck at 2. SYS entity = #8989.518 IEN **4.2** (national). **Proven install→verify→uninstall→clean on BOTH engines** (vehu YDB + foia-t12 IRIS, `testdata/zzparam`).
- mixed-build-split-reversal (in the central docs repo: `docs/proposals/v-pkg-mixed-build-split-reversal.md`) — **PROPOSED 2026-06-28 (not built).** v-pkg's class-aware uninstall handles a build that is EITHER greenfield-add OR pure-overwrite; the high-risk RPC-broker splice is BOTH (overwrites foreign national `XWBPRS` + adds greenfield `VSLRT*`). For that MIXED shape NO single back-out is correct: default `actDelete` (`lifecycle.go:684`) **deletes the live national XWBPRS**; `actRestore` orphans the added routines. `ClassifyBuild` (`reversibility.go:96`) can't see the overwrite (static, side-effects-only) → classes the mixed build as class-1 PureOverwrite → brick. Proposal: D1 `foreignRoutines:["XWBPRS"]` buildspec declaration + F1 build-time guard; D2 mixed reversibility shape (OverwriteForeign/GreenfieldAdd partition, offline-decidable); D3 new `actSplitBackout` (restore-foreign FIRST then delete-greenfield-only, ordered) + mixed-no-pre-image → REFUSE (never delete-all a foreign routine); D4 national-overwrite verify (command-byte vs #9.8 gold + line-2 #63/patch provenance). **N-5 correction: line 2 is NOT checksum-audited (RSUM/RSUM1 exclude it) → v-pkg's line-2-blind drift IS aligned with the checksum; line-2 verify is for patch-history provenance, a separate claim.** Stays inside the v-pkg lifecycle — NO bespoke installer ([[bespoke-installer-forbidden]]). Extends [[class-aware-uninstall]]/[[reversibility-classifier]]/[[snapshot-restore]].
